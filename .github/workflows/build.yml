name: Build Cross-Platform (Qt + OpenCV)

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:
    name: Build on ${{ matrix.os }}
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: windows-latest
            qt_arch: win64_msvc2022_64
            qt_ver: 6.6.0
          - os: macos-latest
            qt_arch: clang_64
            qt_ver: 6.6.0

    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      # 1. FIX: Manually setup Python 3.10 to prevent 'exit code 1' in Windows
      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'

      # 2. Install Qt
      - name: Install Qt
        uses: jurplel/install-qt-action@v3
        with:
          version: ${{ matrix.qt_ver }}
          arch: ${{ matrix.qt_arch }}
          setup-python: 'false' # Prevents the action from conflicting with the Python step above
          extra: '--external 7z' # Faster/more stable extraction on Windows

      # 3. Install OpenCV
      - name: Install OpenCV (Windows)
        if: runner.os == 'Windows'
        run: vcpkg install opencv4:x64-windows

      - name: Install OpenCV (macOS)
        if: runner.os == 'macOS'
        run: brew install opencv

      # 4. Configure CMake
      # Using the vcpkg toolchain for Windows to find OpenCV automatically
      - name: Configure CMake
        run: |
          mkdir build
          cmake -S . -B build `
            -DCMAKE_BUILD_TYPE=Release `
            ${{ runner.os == 'Windows' && '-DCMAKE_TOOLCHAIN_FILE=C:/vcpkg/scripts/buildsystems/vcpkg.cmake' || '' }}

      # 5. Build Project
      - name: Build Project
        run: cmake --build build --config Release

      # 6. Packaging (Deployment)
      - name: Package Windows
        if: runner.os == 'Windows'
        run: |
          windeployqt build/Release/Pebble.exe
          powershell Compress-Archive -Path build/Release/* -DestinationPath Pebble-Windows.zip

      - name: Package macOS
        if: runner.os == 'macOS'
        run: |
          macdeployqt build/Pebble.app -dmg
          mv build/Pebble.dmg Pebble-macOS.dmg

      # 7. Upload Artifacts
      - name: Upload Artifacts
        uses: actions/upload-artifact@v4
        with:
          name: Pebble-${{ runner.os }}
          path: |
            *.zip
            *.dmg